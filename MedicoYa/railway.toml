[build]
builder = "dockerfile"

[deploy]
preDeployCommand = "npm --workspace packages/api exec prisma migrate deploy"
startCommand = "npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
